<h1>{{model.id}}</h1>

<div class="note-body">
  {{model.content}}
  {{textarea value=model.content}}

  {{#if model.hasDirtyAttributes}}
    <button {{action 'saveNote' on='click'}}>Save Note</button>
  {{/if}}
</div>
